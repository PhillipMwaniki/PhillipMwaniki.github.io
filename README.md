# phillipmwaniki.github.io

Personal site — hand-written HTML, CSS and vanilla JS. No framework, no build step.

```
index.html      markup + JSON-LD
css/style.css   styles (light/dark via CSS custom properties)
js/main.js      theme toggle, mobile nav, scrollspy
img/            portrait + favicon
```

Deployed by GitHub Pages from `master`.
